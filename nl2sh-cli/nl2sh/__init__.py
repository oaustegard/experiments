"""nl2sh — hybrid search over shell documentation, with an optional generator.

The search tier is the product. A model is an upgrade, local or remote, and
`backend='none'` is the supported default rather than a fallback.
"""
__version__ = "0.1.0"
