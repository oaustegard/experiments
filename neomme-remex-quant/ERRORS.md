# Error log — neomme-remex-quant

Errors found in this run, how each was caught, and which direction it pushed the
conclusion. Direction first: an error that makes a result look stronger is the
dangerous kind.

| # | Error | Caught by | Direction | Fixed |
|---|---|---|---|---|
| 1 | `neomme_quant.py` imported a `remax.packing.unpack` that does not exist in remax 0.2.0 (assumed from the module name) | First run of the self-test | None — import-time crash, no number produced | Yes — removed; the ±1 decode uses `np.unpackbits` directly |
| 2 | Smoke bench (`results_smoke.json`, 224 docs) ran on the *longest-first* checkpoints, so its bytes/doc (345 KB fp32 late) is 1.8x the corpus mean and its nDCG is over 12 queries | Known at design time; the smoke set is for exercising code paths | Would have overstated late-interaction storage cost by ~1.8x if quoted | Not a result — `results_smoke.json` is not committed and no number from it appears in `RESULTS.md` |
| 3 | `bench.py` kept every late arm's decoded matrix and codec caches alive through the arm list; on ShiftProject (3M tokens, 1.5 GB per remex arm) the first bench was OOM-killed at the third remex arm (exit 137) | Background-task output; `bench_shift.log` ended mid-arm | None on numbers — arms already recorded were correct | Yes — `_release()` drops `T`/`cv`/`codes` after each arm and after each pipeline late index |
| 4 | On a resumed bench, the fidelity reference (`ref_late`) was taken from the first arm computed in the rerun (remex 1-bit) instead of fp32, so seven ShiftProject arms carried top-10 overlaps against the wrong ruler | Read the rerun log: `late remex 1-bit` had `fid@10 None` and later arms had implausibly low overlap | Would have understated fidelity for those seven arms; nDCG unaffected | Yes — the seven arms were pruned from `results_shift.json` and rerun after `bench.py` learned to rebuild the fp32 reference on resume |
| 5 | The prior-art check read the model card, the Hub siblings and derivative repos, and not the paper the card links (nor its kernels repo, hcompai/late-interaction-kernels). The paper's abstract and section 5.9 report token pooling + asymmetric quantization (int8/binary) on the late-interaction head: 255× at 95% retained. The blog post and the first RESULTS.md framed NeoMME as shipping Matryoshka and ignoring quantization | Oskar, reading the paper's summary, 2026-09-08 | Made the token-head half of the study look novel when it is a replication with a different codec; overstated the "vendor ignores quantization" framing | Yes — RESULTS.md prior-art correction, blog correction (lede + a "What the paper already had" section), a correction reply on Bluesky, and the `upstream-prior-art-check` ops entry now requires the linked paper's abstract |
