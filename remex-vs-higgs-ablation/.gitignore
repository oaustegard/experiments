# Regenerable: downloaded datasets, cached embeddings, trained grids, HF cache.
# Everything here is rebuilt by `build_corpora.py` + `pretrain_grids.py`.
assets/
data/
*.hdf5
__pycache__/
sweep*.log
nf_encode.log
