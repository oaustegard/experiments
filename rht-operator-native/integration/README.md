# integration — remex `feat/rht-operator` before merge

Public-API comparison of remex `main` and the branch that implements remex#86
items 1 (operator-form `rht`) and 3 (float32-derived boundaries). See
`bench.py`, `run_all.sh`, `compare.py`, and
`.github/workflows/remex-rht-integration.yml`. Results: `ci/<remex sha>/`,
summarised in `RESULTS.md` once measured.
