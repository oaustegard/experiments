# msd-vocab-weights

Orphan branch holding the adapted ettin-32m checkpoints from `msd-context-classifier` (see `msd-context-classifier/vocab/README.md` on main). Shards under 100 MB; load with `AutoModel.from_pretrained("<dir>")`.
