<!-- Generated by `python3 gemini_arms.py prompt`. This is the ENTIRE input the
clean-room compilers receive: the 79 routing targets, the 23 structural cues,
the executor semantics and the output format. No queries, no eval rows, no
failure list, no existing rules. Regenerate rather than edit. -->

You are compiling a deterministic router for a GitHub tool catalogue.

A user types a natural-language request in a coding assistant. Your job is to write ordered regular-expression rules that map that request to exactly one of the routing targets below, or decline.

## The targets (79 of them)

- actions_get (required: method, owner, repo, resource_id) — Get details about specific GitHub Actions resources. Use this tool to get details about individual workflows, workflow runs, jobs, and artifacts by their unique IDs.
    * actions_get::get_workflow
    * actions_get::get_workflow_run
    * actions_get::get_workflow_job
    * actions_get::download_workflow_run_artifact
    * actions_get::get_workflow_run_usage
    * actions_get::get_workflow_run_logs_url
- actions_list (required: method, owner, repo) — Tools for listing GitHub Actions resources. Use this tool to list workflows in a repository, or list workflow runs, jobs, and artifacts for a specific workflow or workflow run.
    * actions_list::list_workflows
    * actions_list::list_workflow_runs
    * actions_list::list_workflow_jobs
    * actions_list::list_workflow_run_artifacts
- actions_run_trigger (required: method, owner, repo) — Trigger GitHub Actions workflow operations, including running, re-running, cancelling workflow runs, and deleting workflow run logs.
    * actions_run_trigger::run_workflow
    * actions_run_trigger::rerun_workflow_run
    * actions_run_trigger::rerun_failed_jobs
    * actions_run_trigger::cancel_workflow_run
    * actions_run_trigger::delete_workflow_run_logs
- add_comment_to_pending_review (required: owner, repo, pullNumber, path, body, subjectType) — Add review comment to the requester's latest pending pull request review. A pending review needs to already exist to call this (check with the user if not sure).
- add_issue_comment (required: owner, repo, issue_number) — Add a comment and/or reaction to a specific issue or issue comment in a GitHub repository. Use this tool with pull requests as well (in this case pass pull request number as issue_number), but only if user is not asking specifically to add or react to review comments. At least one of body or reaction is required.
- add_reply_to_pull_request_comment (required: owner, repo, commentId) — Add a reply and/or reaction to an existing pull request comment. This can create a new comment linked as a reply to the specified comment, add an emoji reaction to the specified comment, or do both. At least one of body or reaction is required.
- assign_copilot_to_issue (required: owner, repo, issue_number) — Assign Copilot to a specific issue in a GitHub repository. This tool can help with the following outcomes: - a Pull Request created with source code changes to resolve the issue More information can be found at: - https://docs.github.com/en/copilot/concepts/agents/cloud-agent/about-cloud-agent
- create_branch (required: owner, repo, branch) — Create a new branch in a GitHub repository
- create_or_update_file (required: owner, repo, path, content, message, branch) — Create or update a single file in a GitHub repository. If updating, you should provide the SHA of the file you want to update. Use this tool to create or update a file in a GitHub repository remotely; do not use it for local file operations. In order to obtain the SHA of original file version before updating, use the following git command: git rev-parse <branch>:<path to file> SHA MUST be provided for existing file updates.
- create_pull_request (required: owner, repo, title, head, base) — Create a new pull request in a GitHub repository.
- create_repository (required: name) — Create a new GitHub repository in your account or specified organization
- delete_file (required: owner, repo, path, message, branch) — Delete a file from a GitHub repository
- fork_repository (required: owner, repo) — Fork a GitHub repository to your account or specified organization
- get_commit (required: owner, repo, sha) — Get details for a commit from a GitHub repository
- get_file_contents (required: owner, repo) — Get the contents of a file or directory from a GitHub repository
- get_job_logs (required: owner, repo) — Download logs for a specific workflow job or efficiently get all failed job logs for a workflow run
- get_label (required: owner, repo, name) — Get a specific label from a repository.
- get_latest_release (required: owner, repo) — Get the latest release in a GitHub repository
- get_me (required: none) — Get details of the authenticated GitHub user. Use this when a request is about the user's own profile for GitHub. Or when information is missing to build other tool calls.
- get_release_by_tag (required: owner, repo, tag) — Get a specific release by its tag name in a GitHub repository
- get_tag (required: owner, repo, tag) — Get details about a specific git tag in a GitHub repository
- get_team_members (required: org, team_slug) — Get member usernames of a specific team in an organization. Limited to organizations accessible with current credentials
- get_teams (required: none) — Get details of the teams the user is a member of. Limited to organizations accessible with current credentials
- issue_read (required: method, owner, repo, issue_number) — Get information about a specific issue in a GitHub repository.
    * issue_read::get
    * issue_read::get_comments
    * issue_read::get_sub_issues
    * issue_read::get_parent
    * issue_read::get_labels
    method notes: The read operation to perform on a single issue. Options are: 1. get - Get issue details. Also returns best-effort hierarchy flags (`has_parent`, `has_children`); `parent` and `sub_issues_summary` are optional relationship summaries, and `c
- issue_write (required: method, owner, repo) — Create a new or update an existing issue in a GitHub repository.
    * issue_write::create
    * issue_write::update
    method notes: Write operation to perform on a single issue. Options are: - 'create' - creates a new issue. - 'update' - updates an existing issue.
- list_branches (required: owner, repo) — List branches in a GitHub repository
- list_commits (required: owner, repo) — Get list of commits of a branch in a GitHub repository. Returns at least 30 results per page by default, but can return more if specified using the perPage parameter (up to 100).
- list_issue_fields (required: owner) — List issue fields for a repository or organization. Returns field definitions including name, type (text, number, date, single_select), and for single_select fields the list of valid option names. When repo is omitted, returns org-level fields directly.
- list_issue_types (required: owner) — List supported issue types for a repository or its owner organization. When repo is omitted, returns org-level issue types directly.
- list_issues (required: owner, repo) — List issues in a GitHub repository. For pagination, use the 'endCursor' from the previous response's 'pageInfo' in the 'after' parameter.
- list_pull_requests (required: owner, repo) — List pull requests in a GitHub repository. If the user specifies an author, then DO NOT use this tool and use the search_pull_requests tool instead.
- list_releases (required: owner, repo) — List releases in a GitHub repository
- list_repository_collaborators (required: owner, repo) — List collaborators of a GitHub repository. Results are paginated; the response includes `nextPage`, `prevPage`, `firstPage`, and `lastPage` fields. To get the next page, use the `nextPage` value as the `page` parameter.
- list_tags (required: owner, repo) — List git tags in a GitHub repository
- merge_pull_request (required: owner, repo, pullNumber) — Merge a pull request in a GitHub repository.
- pull_request_read (required: method, owner, repo, pullNumber) — Get information on a specific pull request in GitHub repository.
    * pull_request_read::get
    * pull_request_read::get_diff
    * pull_request_read::get_status
    * pull_request_read::get_files
    * pull_request_read::get_commits
    * pull_request_read::get_review_comments
    * pull_request_read::get_reviews
    * pull_request_read::get_comments
    * pull_request_read::get_check_runs
    method notes: Action to specify what pull request data needs to be retrieved from GitHub. Possible options: 1. get - Get details of a specific pull request. 2. get_diff - Get the diff of a pull request. 3. get_status - Get combined commit status of a hea
- pull_request_review_write (required: method, owner, repo, pullNumber) — Create and/or submit, delete review of a pull request. Available methods: - create: Create a new review of a pull request. If "event" parameter is provided, the review is submitted. If "event" is omitted, a pending review is created. - submit_pending: Submit an existing pending review of a pull request. This requires that a pending review exists for the current user on the specified pull request. The "body" and "event" parameters are used when submitting the review. - delete_pending: Delete an existing pending review of a pull request. This requires that a pending review exists for the current
    * pull_request_review_write::create
    * pull_request_review_write::submit_pending
    * pull_request_review_write::delete_pending
    * pull_request_review_write::resolve_thread
    * pull_request_review_write::unresolve_thread
    method notes: The write operation to perform on pull request review.
- push_files (required: owner, repo, branch, files, message) — Push multiple files to a GitHub repository in a single commit
- request_copilot_review (required: owner, repo, pullNumber) — Request a GitHub Copilot code review for a pull request. Use this for automated feedback on pull requests, usually before requesting a human reviewer.
- resolve_review_thread (required: threadID) — Resolve a review thread on a pull request. Resolving an already-resolved thread is a no-op.
- search_code (required: query) — Fast and precise code search across ALL GitHub repositories using GitHub's native search engine. Best for finding exact symbols, functions, classes, or specific code patterns.
- search_commits (required: query) — Search for commits across GitHub repositories using GitHub's commit search syntax. Useful for finding specific changes, authors, or messages across one or many repositories. Searches the default branch only.
- search_issues (required: query) — Search issues using natural-language semantic matching. Best for conceptual or paraphrased queries (e.g. "login fails after password reset"). Already scoped to is:issue.
- search_pull_requests (required: query) — Search for pull requests in GitHub repositories using issues search syntax already scoped to is:pr
- search_repositories (required: query) — Find GitHub repositories by name, description, readme, topics, or other metadata. Perfect for discovering projects, finding examples, or locating specific repositories across GitHub.
- search_users (required: query) — Find GitHub users by username, real name, or other profile information. Useful for locating developers, contributors, or team members.
- sub_issue_write (required: method, owner, repo, issue_number, sub_issue_id) — Add a sub-issue to a parent issue in a GitHub repository.
- unresolve_review_thread (required: threadID) — Unresolve a previously resolved review thread on a pull request. Unresolving an already-unresolved thread is a no-op.
- update_pull_request (required: owner, repo, pullNumber) — Update an existing pull request in a GitHub repository.
- update_pull_request_branch (required: owner, repo, pullNumber) — Update the branch of a pull request with the latest changes from the base branch.

## Structural cues available to you

A separate extraction layer has already parsed the request and can tell you whether each of these is present. Use them as preconditions so that a keyword alone cannot fire a rule that needs a structural referent:

- url_pull
- url_issue
- url_blob
- url_commit
- url_run
- url_release
- url_repo
- pr_ref
- issue_ref
- hash_num
- sha
- sha40
- run_ref
- path
- workflow_file
- semver_tag
- owner_repo
- at_user
- quoted
- thread_id
- org_team
- bare_num
- branch_word

## How the rules are executed

Rules are tried in order. The first rule whose `requires` cue is present (if it declares one) AND whose `pattern` matches the request wins. If no rule matches, the router ABSTAINS — this is correct and desirable for requests no tool serves. There is no fallback rule and you must not write one: a catch-all destroys the ability to decline.

## What to produce

A JSON array. Each element:

  {"label": "<exact target from the list>",
    "pattern": "<Python regex, case-insensitive, or \"\" to match anything>",
    "requires": "<one cue name from the list, or null>"}

Rules to follow:
- Order matters. Put specific rules before general ones.
- Enumerate synonyms people actually use. A request for a diff might say diff, patch, changeset, "what changed", "what code does this change".
- Prefer a structural precondition wherever the target needs a referent.
- Cover the targets you believe requests will actually hit. Omitting a target is better than a rule you do not believe in, because a wrong rule earlier in the order blocks every rule after it.
- Grammatical number is meaningful here: plural usually names a list endpoint, singular a fetch-one.

Output ONLY the JSON array. No prose, no markdown fence.
